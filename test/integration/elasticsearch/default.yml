---
- name: Configure ElasticSearch
  hosts: all
  become: true
  become_user: root
  become_method: sudo
  vars:
    elasticsearch_bind: 0.0.0.0
  roles:
    - { role: java, java_type: openjdk, java_version: 8 }
    - { role: ansible-role-elasticsearch }
